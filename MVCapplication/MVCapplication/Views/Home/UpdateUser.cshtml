@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@model UpdateUser
@{
    ViewData["Title"] = "Profile";
}

<html>

<head>

    <script type="text/javascript">
        var user =@User.Identity.Name;
    </script>
</head>

<body>
    
    <br />
    <h3>User Management</h3>
    <div class="row">
        <div class="col-md-3">
            <ul class="nav nav-pills flex-column">
                <li class="nav-item active"><a class="nav-link active" asp-action="UpdateUser">Profile</a></li>
                <li class="nav-item"><a class="nav-link" asp-action="UpdatePassword">Change Password</a></li>
            </ul>
        </div>

        <div class="col-md-9">
            @if (ViewBag.status != null)
            {
                <div class="alert alert-success" style="width:500px;margin:auto;" role="alert">
                    @ViewBag.status
                </div>
            }
            @if (ViewBag.error != null)
            {
                <div class="alert alert-danger" style="width:500px;margin:auto;" role="alert">
                    @ViewBag.error
                </div>
            }
            @if (ViewBag.nameandphone != null)
            {
                <h3>Personal Details</h3>
                <form id="frmDetail" method="post" action="UpdateRecord">
                    <div class="form-group">
                        <input type="hidden" name="txtuser" asp-for="@User.Identity.Name" class="form-control" readonly />
                        <span asp-validation-for="@User.Identity.Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label>Name</label>
                        <input name="txtFName" value="@ViewBag.nameandphone[0]" asp-for="Input.FullName" class="form-control" required />
                        <span asp-validation-for="Input.FullName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Input.Email"></label>
                        <input name="txtEmail" value="@User.Identity.Name" asp-for="Input.Email" class="form-control" readonly/>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Input.PhoneNumber"></label>
                        <input name="txtPhno" value="@ViewBag.nameandphone[1]" asp-for="Input.PhoneNumber" class="form-control" required />
                        <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
                    </div>
                    <input type="submit" class="btn btn-info" value="Update" name="action" />
                </form>
            }
        </div>


    </div>

</body>

</html>











<!--
  <label>User Name</label><input size="150" name="txtuser" asp-for="User.Identity.Name" value="User.Identity.Name" asp-validation-for="Input.UserName" readonly />
  <br />
  <label>First Name</label><input size="150" name="txtfName" asp-for="Input.FullName" asp-validation-for="Input.FullName" />
  <br />
  <label>Last Name</label><input size="150" name="txtlName" />
  <br />
  <label>Email Address</label><input size="150" name="txtEmail" asp-for="Input.Email" asp-validation-for="Input.Email" />
  <br />
  <label>Change Password</label><input size="150" name="txtPswd" asp-for="Input.Password" asp-validation-for="Input.Password" />
  <br />
  <label>Contact Number</label><input size="150" name="txtPhno" asp-for="Input.PhoneNumber" asp-validation-for="Input.UserName" />
  <br />  <br />
  <input type="submit" class="btn btn-info" value="Update" name="action" />

  <br />
      -->
